<%= form_for(@paticipant) do |f| %>
  <% if @paticipant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@paticipant.errors.count, "error") %> prohibited this paticipant from being saved:</h2>

      <ul>
      <% @paticipant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.hidden_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
